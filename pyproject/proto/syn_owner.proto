syntax = "proto2";

// 同步自己信息 20030
message syn_owner_info
{
    required uint32     ret = 1;
    required uint64     player_id = 2;
    required bytes      wechat_info = 3;
    repeated bytes      list_money = 4;
    required uint32     cur_in_room_id = 5; // 当前开局房间id
};